// const systemList = document.querySelectorAll('.system_list li');
// console.log(systemList);

// // querySelectorAll은 NodeList를 반환하는 '유사배열'이기 때문에 setAttribute 직접 사용 불가
// // systemList.setAttribute('data-aos', 'fade-left');

// systemList.forEach(function(dlElement) {
//   dlElement.setAttribute('data-aos', 'fade-left');
// })

// AOS.refresh();